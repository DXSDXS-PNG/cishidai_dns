# 贡献说明

感谢你愿意参与这个项目。这个项目的目标是保持简单、可部署、易二开，优先服务普通域名分发和 DNS 解析场景。

## 如何贡献

你可以通过以下方式参与：

- 提交 Bug 反馈
- 提交功能建议
- 改进文档和部署教程
- 修复代码问题
- 适配新的 DNS 服务商
- 优化前端交互和界面体验

## 提交 Issue

提交问题时，建议包含：

- 运行环境：PHP、MySQL、Nginx/Apache 版本
- 复现步骤
- 期望结果
- 实际结果
- 报错截图或日志

如果是功能建议，请说明使用场景，尽量避免只写“希望增加某功能”。

## 提交 Pull Request

提交 PR 前请尽量做到：

- 保持改动聚焦，一次 PR 解决一个问题
- 不提交真实密钥、账号、数据库备份、线上数据
- 不引入和普通域名分发无关的复杂业务
- PHP 文件通过语法检查
- 前端 JS 文件通过基础语法检查

推荐检查命令：

```bash
php -l api.php
php -l index.php
node --check tenant.js
```

## 代码风格

- PHP 代码保持清晰直接，避免过度抽象
- 前端优先保持现有页面结构和交互习惯
- 数据库变更需要同步更新 `install.sql`
- 新增配置项需要提供合理默认值

## 不建议提交的内容`r`n`r`n为了让项目保持轻量，本项目不建议加入与核心 DNS 分发无关的大型依赖或复杂业务模块

## 安全问题

如果你发现安全问题，请优先通过私下方式联系维护者，不建议直接公开漏洞细节。

维护者联系方式可在 `README.md` 中填写。
